import { FlowShowcase } from "@/components/sections/FlowShowcase";

export function FinancialProfile() {
  return (
    <FlowShowcase
      id="financial-profile"
      label="Your Financial Profile"
      title="Track Your Financial Health Score Instantly."
      body="Veris starts with your financial profile — balance, income, expenses, and active goals — so every recommendation reflects your true financial health score today."
      screen="dashboard"
    />
  );
}
