import { FlowShowcase } from "@/components/sections/FlowShowcase";

export function FinancialProfile() {
  return (
    <FlowShowcase
      id="financial-profile"
      label="Your Financial Profile"
      title="One complete picture of your money."
      body="Veris starts with your financial profile — balance, income, expenses, and active goals — so every recommendation reflects where you actually stand today."
      screen="dashboard"
    />
  );
}
